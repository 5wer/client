import { Vue } from 'vue-property-decorator';

export default class Slider extends Vue {
  render() {
    return (
      <div class="about">
        <h1>slider</h1>
      </div>
    );
  }
}
