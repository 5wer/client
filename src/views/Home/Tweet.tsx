import { Vue } from 'vue-property-decorator';

export default class Tweet extends Vue {
  render() {
    return (
      <div class="about">
        <h1>tweet</h1>
      </div>
    );
  }
}
